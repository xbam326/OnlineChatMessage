# チャットルームアプリケーション開発ログ

## 概要
TCP/UDPを使用したチャットルームアプリケーションの段階的開発記録

## 実装済み機能（Step 1: TCPによるルーム作成機能）

### 実装内容
1. **TCRPプロトコル定義** (`protocol.py`)
   - TCRPヘッダー（32バイト）の実装
   - Operation（CREATE=1, JOIN=2）とState（REQUEST=0, RESPONSE=1, COMPLETE=2）の定義
   - TCPパケットのエンコード/デコード機能

2. **TCPサーバー機能** (`server.py`)
   - ポート9001 でTCPサーバーを起動
   - チャットルーム管理クラス（ChatRoom）
   - ルーム作成リクエストの処理
   - トークン生成 (uuid使用)

3. **TCPクライアント機能** (`client.py`)
   - ルーム作成機能（create_room関数）
   - TCPサーバーへの接続とリクエスト送信
   - トークン受信と保存

### テスト結果
- ✅ TCPサーバー起動成功
- ✅ ルーム作成リクエスト送信成功
- ✅ トークン生成と受信成功

## 今後の実装計画

### Step 2: ホスト単体でのUDPチャット機能
**目標**: 作成したルームでホストがUDPを使ってチャット

**実装タスク**:
1. UDPメッセージにルーム名・トークンヘッダー追加
   - `UDPMessageFormat.pack()`の活用
   - ヘッダー: RoomNameSize（1バイト）| TokenSize（1バイト）

2. サーバー側のUDP処理改修
   - トークン検証機能の追加
   - ルーム単位でのメッセージ管理
   - 認証済みクライアントのみへの配信

3. クライアント側のUDP処理改修
   - トークン付きメッセージ送信
   - 新フォーマットでの受信処理

**テスト項目**:
- ホストがルーム内でメッセージ送受信可能か
- トークンなしのメッセージが拒否されるか
- 異なるルーム間でメッセージが混ざらないか

### Step 3: 他ユーザーの参加機能
**目標**: JOIN操作で他のユーザーがルームに参加

**実装タスク**:
1. JOIN操作の実装
   - `Operation.JOIN`のハンドラー追加
   - 参加者リスト管理
   - 参加者用トークン生成

2. ルーム参加フロー
   - ルーム名の入力と検証
   - 参加リクエスト送信
   - トークン受信と保存

3. メッセージブロードキャスト
   - ルーム内全メンバーへの配信
   - 参加/退出通知

**テスト項目**:
- 複数クライアントの同時接続
- ルーム内でのメッセージ共有
- 存在しないルームへの参加エラー処理

### Step 4: トークン認証とセキュリティ
**目標**: IPアドレス検証とホスト退出処理

**実装タスク**:
1. IPアドレスとトークンの紐付け
   - トークン使用時のIP検証
   - 不正アクセスの防止

2. ホスト退出処理
   - ルーム自動削除
   - 参加者への通知
   - リソースのクリーンアップ

3. タイムアウト処理
   - 非アクティブユーザーの削除
   - 接続状態の監視

**テスト項目**:
- 異なるIPからのトークン使用拒否
- ホスト退出時のルーム削除
- タイムアウト処理の動作確認

### Step 5: エラー処理と再接続
**目標**: 堅牢性の向上

**実装タスク**:
1. 包括的なエラーハンドリング
   - ネットワークエラー
   - プロトコルエラー
   - タイムアウトエラー

2. 自動再接続機能
   - 切断検出
   - 再参加フロー
   - 状態の復元

3. ユーザビリティ改善
   - わかりやすいエラーメッセージ
   - 進捗表示
   - ヘルプ機能

---

## テストファイル一覧
開発中に作成したテストファイル（コミット前に削除予定）:
- `test_server.py` - サーバーのエラーハンドリングテスト
- `test_client_room_create.py` - ルーム作成機能のテスト
- `debug_packet.py` - パケットエンコーディングのデバッグ
- `debug_client.py` - クライアント側パケット解析のデバッグ

## 環境情報
- Python 3.9.20
- macOS Darwin 23.0.0
- 使用ポート:
  - TCP: 9000
  - UDP: 9001
  - クライアントUDP: 9002-9100（ランダム）

## 注意事項
- サーバーはマルチスレッドで動作（TCP/UDP同時処理）
- トークンは`secrets.token_urlsafe(32)`で生成（安全性確保）
- ルーム名は最大28バイト（UTF-8エンコード後）
- メッセージは最大4096バイト

